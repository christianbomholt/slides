

install.packages("ggplot2")
install.packages("tidyr")
install.packages(c("dplyr","readxl"))

diamonds

library(ggplot2)

diamonds

library(dplyr)

library()

diamonds <- mutate(diamonds, price_pr_carat = price / carat)

?dplyr
